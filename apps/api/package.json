{
  "name": "@ai-context/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ai-context/contracts": "workspace:*",
    "@ai-context/shared": "workspace:*",
    "@ai-context/types": "workspace:*"
  }
}
