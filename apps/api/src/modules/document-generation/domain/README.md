# Document Generation Domain

The Document Generation domain is persistence-neutral and framework-independent.

It defines the minimal Document model, supported MVP document type and format values, generation
input and output boundaries, and generator/renderer contracts.
