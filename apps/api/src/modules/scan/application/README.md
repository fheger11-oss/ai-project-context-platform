# Scan Application Layer

Future use cases and business orchestration for repository snapshot management belong here.

`ScanService` defines the application boundary for starting a future repository snapshot without implementing scanning. It depends only on Scan domain contracts.
